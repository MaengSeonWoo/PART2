<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.talk.app.admin.welfare.mapper.WelfareMapper">

<insert id="insertServId" parameterType="WelfareVO">
        INSERT INTO welfare (serv_id)
        VALUES (#{servId})

</insert>


<select id="getAllServId" resultType="WelfareVO">
        SELECT serv_id AS servId FROM welfare
</select>

<update id="updateDetailInfo" parameterType="WelfareVO">
        UPDATE welfare
        SET 
            serv_name = #{servName},
            start_date = #{startDate},
            end_date = #{endDate},
            biz_dept = #{bizDept},
            sido = #{sido},
            sgg = #{sgg},
            serv_summary = #{servSummary},
            household = #{household},
            like_subject = #{likeSubject},
            sup_period = #{supPeriod},
            prov_type = #{provType},
            app_way = #{appWay},
            sup_target = #{supTarget},
            sel_standard = #{selStandard},
            sal_serv = #{salServ}
        WHERE serv_id = #{servId}1
</update>


</mapper>